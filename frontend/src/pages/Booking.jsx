import { PagePlaceholder } from '../components/PagePlaceholder';

export default function Booking() {
  return (
    <PagePlaceholder
      title="Book your car"
      blurb="The step-by-step booking flow (dates → availability → quote → confirm) is being built."
    />
  );
}
